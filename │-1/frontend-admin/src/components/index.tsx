import React from 'react';

const AdminComponents = () => {
    return (
        <div>
            <h1>Componentes do Painel Administrativo</h1>
            {/* Adicione aqui os componentes reutilizáveis do painel administrativo */}
        </div>
    );
};

export default AdminComponents;